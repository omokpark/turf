requests>=2.31
pandas>=2.0
python-dotenv>=1.0
streamlit>=1.40      # Day 3 (Day 14: 화면 전환에 st.segmented_control 사용 — 1.40+)
folium>=0.15         # Day 3
streamlit-folium>=0.18,<0.28 # Day 3. 상한 고정(Phase 3): 지도 상호작용 JS가 프론트엔드 내부(map_div 변수명, onLayerClick 툴팁 반환)에 의존 — 0.27.x에서 검증됨
pyproj>=3.6          # Day 9: 인허가 좌표(EPSG:5174) → WGS84 변환
pyarrow>=15.0        # Day 10(Phase 1): 인허가 파티션 parquet 캐시
pytest>=8.0          # Day 10(Phase 1): 테스트 기반
rapidfuzz>=3.0       # Day 11(Phase 4): 상호명 유사도 매칭
watchdog>=4.0        # Day 14: Streamlit 파일 감시 — 폴링 감시는 모듈 변경 리로드를 놓친다 (서버 재시작 필요했던 원인)
google-genai>=1.0    # Day 17: 화면별 챗봇 (Gemini 무료 티어, 컨텍스트 스터핑)
